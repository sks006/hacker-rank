#include <stdio.h>
int main()
{
    int a = 0, b = 0, n = 0;
    char* arr[10] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};
    scanf("%d\n%d", &a, &b);
    for (int n = a; n <= b; n++)
    {
        if (n > 9)
        {
            if (n % 2 == 0)
            {
                printf("even/n");
            }
            else
            {
                printf("odd/n");
            }
        }
          else
        {
        puts(arr[n]);
        }
    }
  

    return 0;
}
